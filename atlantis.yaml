# =============================================================================
# atlantis.yaml — Repo-level Atlantis configuration
# =============================================================================
version: 3

# ─── PROJECTS ────────────────────────────────────────────────────────────────
# Each project can be triggered individually from a PR comment.
# Use `atlantis plan -p <name>` to target a specific project.
projects:

  # ── Main project: full scan + terraform plan + apply ─────────────────────
  # Triggered automatically on PR push (autoplan) or with: atlantis plan
  - name: alantis-demo
    dir: .
    workspace: default              # default workspace = main state file
    workflow: all-scans-plan        # runs all 3 scanners + init + plan
    apply_requirements:
      - approved                    # PR must be approved before atlantis apply
    autoplan:
      when_modified:
        - "*.tf"
        - "*.tfvars"
        - "atlantis.yaml"
      enabled: true

  # ── KICS scan only ────────────────────────────────────────────────────────
  # Trigger with: atlantis plan -p scan-kics
  # This only runs KICS, no terraform plan is generated.
  - name: scan-kics
    dir: .
    workspace: kics                 # separate workspace to avoid lock conflicts
    workflow: kics-only
    autoplan:
      enabled: false                # never auto-trigger; only on explicit command

  # ── tfsec scan only ───────────────────────────────────────────────────────
  # Trigger with: atlantis plan -p scan-tfsec
  - name: scan-tfsec
    dir: .
    workspace: tfsec
    workflow: tfsec-only
    autoplan:
      enabled: false

  # ── Checkov scan only ─────────────────────────────────────────────────────
  # Trigger with: atlantis plan -p scan-checkov
  - name: scan-checkov
    dir: .
    workspace: checkov
    workflow: checkov-only
    autoplan:
      enabled: false

# ─── WORKFLOWS ────────────────────────────────────────────────────────────────
workflows:

  # ── all-scans-plan: KICS chạy TRƯỚC TIÊN (non-blocking) + export CSV ─────
  all-scans-plan:
    plan:
      steps:
        # ✅ BƯỚC 1: KICS scan — NON-BLOCKING + export CSV/JSON report
        # Dù KICS tìm thấy lỗi, các bước tiếp theo vẫn tiếp tục chạy
        # CSV report được lưu tại: /tmp/kics-report/<timestamp>-PR<num>/
        - run: |
            echo "=== [1/5] KICS security scan (non-blocking + CSV export) ==="
            REPORT_DIR="/tmp/kics-report/$(date +%Y%m%d-%H%M%S)-PR${PULL_NUM}"
            mkdir -p "$REPORT_DIR"
            kics scan \
              -p . \
              -q /app/assets/queries \
              --no-progress \
              --type Terraform \
              --report-formats csv,json \
              --output-path "$REPORT_DIR" \
              --ignore-on-exit results
            echo ""
            echo "📁 Report saved to: $REPORT_DIR"
            echo "   - CSV : $REPORT_DIR/results.csv"
            echo "   - JSON: $REPORT_DIR/results.json"
            echo ""
            if [ -f "$REPORT_DIR/results.csv" ]; then
              TOTAL=$(awk -F',' 'NR>1' "$REPORT_DIR/results.csv" | wc -l)
              echo "🔍 Total findings: $TOTAL"
              echo ""
              echo "| Severity | Rule Name | File | Line |"
              echo "|----------|-----------|------|------|"
              awk -F',' 'NR>1 {printf "| %s | %s | %s | %s |\n", $5, $2, $7, $8}' "$REPORT_DIR/results.csv"
            fi
            echo ""
            echo "=== KICS scan complete ==="

        - run: |
            echo "=== [2/5] tfsec security scan (non-blocking + JSON export) ==="
            REPORT_DIR="/tmp/tfsec-report/$(date +%Y%m%d-%H%M%S)-PR${PULL_NUM}"
            mkdir -p "$REPORT_DIR"
            tfsec . --no-color --soft-fail --format json > "$REPORT_DIR/results.json" 2>&1 || true
            tfsec . --no-color --soft-fail
            echo ""
            echo "📁 tfsec report saved to: $REPORT_DIR/results.json"
            echo "=== tfsec scan complete ==="

        - run: |
            echo "=== [3/5] Checkov security scan (non-blocking + CSV/JSON export) ==="
            REPORT_DIR="/tmp/checkov-report/$(date +%Y%m%d-%H%M%S)-PR${PULL_NUM}"
            mkdir -p "$REPORT_DIR"
            checkov -d . --quiet --compact --framework terraform --soft-fail \
              --output json > "$REPORT_DIR/results.json" 2>&1 || true
            checkov -d . --quiet --compact --framework terraform --soft-fail \
              --output csv > "$REPORT_DIR/results.csv" 2>&1 || true
            checkov -d . --compact --framework terraform --soft-fail
            echo ""
            echo "📁 Checkov report saved to: $REPORT_DIR"
            echo "   - CSV : $REPORT_DIR/results.csv"
            echo "   - JSON: $REPORT_DIR/results.json"
            echo "=== Checkov scan complete ==="

        - init   # Bước 4: terraform init
        - plan   # Bước 5: terraform plan (kết quả post lên PR comment)

    apply:
      steps:
        - apply  # runs terraform apply with the saved plan file

  # ── kics-only: KICS scan only + export CSV, no terraform plan ────────────
  # Comment `atlantis plan -p scan-kics` on the PR to trigger.
  kics-only:
    plan:
      steps:
        - run: |
            echo "=== KICS security scan + CSV export ==="
            REPORT_DIR="/tmp/kics-report/$(date +%Y%m%d-%H%M%S)-PR${PULL_NUM}"
            mkdir -p "$REPORT_DIR"
            kics scan \
              -p . \
              -q /app/assets/queries \
              --no-progress \
              --type Terraform \
              --report-formats csv,json \
              --output-path "$REPORT_DIR" \
              --ignore-on-exit results
            echo ""
            echo "📁 Report saved to: $REPORT_DIR"
            echo "   - CSV : $REPORT_DIR/results.csv"
            echo "   - JSON: $REPORT_DIR/results.json"
            echo ""
            if [ -f "$REPORT_DIR/results.csv" ]; then
              TOTAL=$(awk -F',' 'NR>1' "$REPORT_DIR/results.csv" | wc -l)
              HIGH=$(awk -F',' 'NR>1 && toupper($5)=="HIGH"' "$REPORT_DIR/results.csv" | wc -l)
              MED=$(awk -F',' 'NR>1 && toupper($5)=="MEDIUM"' "$REPORT_DIR/results.csv" | wc -l)
              LOW=$(awk -F',' 'NR>1 && toupper($5)=="LOW"' "$REPORT_DIR/results.csv" | wc -l)
              INFO=$(awk -F',' 'NR>1 && toupper($5)=="INFO"' "$REPORT_DIR/results.csv" | wc -l)
              echo "🔍 Summary: Total=$TOTAL | HIGH=$HIGH | MEDIUM=$MED | LOW=$LOW | INFO=$INFO"
              echo ""
              echo "| Severity | Rule Name | File | Line |"
              echo "|----------|-----------|------|------|"
              awk -F',' 'NR>1 {printf "| %s | %s | %s | %s |\n", $5, $2, $7, $8}' "$REPORT_DIR/results.csv"
            fi
            echo "=== KICS scan complete ==="

  # ── tfsec-only: tfsec scan only, no terraform plan ────────────────────────
  # Comment `atlantis plan -p scan-tfsec` on the PR to trigger.
  tfsec-only:
    plan:
      steps:
        - run: |
            echo "=== tfsec security scan (non-blocking + JSON export) ==="
            REPORT_DIR="/tmp/tfsec-report/$(date +%Y%m%d-%H%M%S)-PR${PULL_NUM}"
            mkdir -p "$REPORT_DIR"
            tfsec . --no-color --soft-fail --format json > "$REPORT_DIR/results.json" 2>&1 || true
            tfsec . --no-color --soft-fail
            echo ""
            echo "📁 tfsec report saved to: $REPORT_DIR/results.json"
            echo "=== tfsec scan complete ==="

  # ── checkov-only: Checkov scan only, no terraform plan ────────────────────
  # Comment `atlantis plan -p scan-checkov` on the PR to trigger.
  checkov-only:
    plan:
      steps:
        - run: |
            echo "=== Checkov security scan (non-blocking + CSV/JSON export) ==="
            REPORT_DIR="/tmp/checkov-report/$(date +%Y%m%d-%H%M%S)-PR${PULL_NUM}"
            mkdir -p "$REPORT_DIR"
            checkov -d . --quiet --compact --framework terraform --soft-fail \
              --output json > "$REPORT_DIR/results.json" 2>&1 || true
            checkov -d . --quiet --compact --framework terraform --soft-fail \
              --output csv > "$REPORT_DIR/results.csv" 2>&1 || true
            checkov -d . --compact --framework terraform --soft-fail
            echo ""
            echo "📁 Checkov report saved to: $REPORT_DIR"
            echo "   - CSV : $REPORT_DIR/results.csv"
            echo "   - JSON: $REPORT_DIR/results.json"
            echo "=== Checkov scan complete ==="
